flyway:
  datasources:
    default:
      enabled: false

jpa:
  default:
    entity-scan:
      classpath: true
    properties:
      hibernate:
        show-sql: true
        hbm2ddl:
          auto: update
        connection:
          db-type: postgres
    reactive: true
